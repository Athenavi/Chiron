// 注意：此配置文件与 .env + config.go 配置体系并存，修改时需同步两处。
// 推荐统一使用 .env 环境变量管理配置。
{
  "port": "8080",
  "storage_backend": "local",
  "log_level": "info",
  "postgres_max_conn": 20,
  "postgres_min_conn": 2,
  "rate_limit_rpm": 100
}

